@import './src/app/util/common';

.tasksContainer {
  width: 100%;
}

.currentWeek {
  padding: $normalGap;

}

.buttons {
  display: flex;
  flex-direction: row;
  padding: $normalGap;
  background: $green;
}

.gap {
  flex-grow: 100;
}